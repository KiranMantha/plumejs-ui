{
  "name": "plumejs-ui",
  "version": "1.0.1",
  "description": "A collection of UI components built using PlumeJs",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "patch-update": "npm version patch",
    "tag": "git push --follow-tags",
    "tag-release": "npm run patch-update && npm run tag",
    "tsc": "tsc && copyfiles src/**/*.scss dist/"
  },
  "main": "./dist/index.js",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bootstrap": "^4.3.1",
    "copyfiles": "^2.2.0",
    "plumejs": "^2.1.4",
    "typescript": "^3.7.2"
  }
}
